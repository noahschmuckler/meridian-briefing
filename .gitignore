node_modules/
.env
.env.local
.dev.vars
.DS_Store
*.log
dist/

# Local state document. The server points BRIEFING_DB at a persistent path;
# locally `npm run seed` writes ./data/state.json, which must never be committed.
/data/
